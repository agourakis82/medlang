# MedLang Compiler (Skeleton)

This directory will contain the MedLang compiler:

- Frontend (parser, typechecker)
- Clinical IR (CIR)
- Numeric IR (NIR)
- MLIR-based lowering to CPU/GPU (LLVM, PTX, etc.)

Status: empty skeleton, to be filled iteratively.
