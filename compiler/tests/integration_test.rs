//! Basic integration test to verify compiler builds and links correctly

#[test]
fn test_library_compiles() {
    // This test simply verifies that the library compiles and can be imported
    // If we can import medlangc, the core compilation was successful
    assert!(true);
}
